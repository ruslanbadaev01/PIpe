import { NewFirstPipePipe } from './new-first-pipe.pipe';

describe('NewFirstPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NewFirstPipePipe();
    expect(pipe).toBeTruthy();
  });
});
